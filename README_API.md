Ini adalah petunjuk untuk menjalankan server API v2.1 Anda.

### Langkah 1: Instalasi Library

Pastikan Anda memiliki library ini (kemungkinan sudah Anda install):

```bash
pip install fastapi "uvicorn[standard]" pandas numpy